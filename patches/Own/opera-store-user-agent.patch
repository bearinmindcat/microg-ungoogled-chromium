diff --git a/net/url_request/url_request_http_job.cc b/net/url_request/url_request_http_job.cc
--- a/net/url_request/url_request_http_job.cc
+++ b/net/url_request/url_request_http_job.cc
@@ -545,6 +545,16 @@
       http_user_agent_settings_ ? http_user_agent_settings_->GetUserAgent()
                                 : std::string());
 
+  {
+    const std::string_view host = request_info_.url.host();
+    if (host == "addons.opera.com" || host.ends_with(".addons.opera.com")) {
+      request_info_.extra_headers.SetHeader(
+          HttpRequestHeaders::kUserAgent,
+          "Mozilla/5.0 (X11; Linux x86_64) AppleWebKit/537.36 (KHTML, like "
+          "Gecko) Chrome/150.0.0.0 Safari/537.36 OPR/118.0.0.0");
+    }
+  }
+
   AddExtraHeaders();
 
   if (ShouldAddCookieHeader()) {
